#include<stdio.h>

typedef struct {
    int bt, ct, wt, tat;
} process;

int  main() {
    int n; printf("Enter the no. of processes : ");     scanf("%d", &n);

    process a[n];
    printf("enter the burst time of processes :\n");
    for(int i = 0; i < n; i++) scanf("%d", &
    )
}