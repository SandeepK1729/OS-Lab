#include<stdio.h>

int main() {
    int b; printf("Enter no of blocks : ");  scanf("%d", &n);
    int blocks[b];
    printf("Enter %d block sizes : "); 
    for(int i = 0; i < b; i++) scanf("%d", blocks + i);

    int f; printf("Enter no of files : ");   scanf("%d", &f);
    int files[f];
    for(int i = 0; i < n; i++) scanf("%d", files + i);

    
}